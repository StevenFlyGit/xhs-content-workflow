# 已知问题与改进清单

本文档记录当前项目在功能完整性、数据一致性、平台接入和可维护性方面的主要问题。内容来源于项目代码解析结果，对应 Annotation 1。

## 1. 来源证据 ID 不匹配（已修复）

**状态：已于 2026-07-28 修复。**

### 原问题

AI 解析结果使用句子 ID，例如 `S02-01`；证据面板主要使用段落 ID，例如 `P02`，查找对应原文。

因此，在执行真实语义解析后，点击观点下方的“来源”可能无法显示相应原文。内置示例使用段落 ID，所以这个问题在初始演示数据中不一定出现。

### 修复方式

- 新增统一的 `SourceSentence` 数据模型，集中生成句子 ID、段落 ID、原文和顺序；
- 服务端解析结果中的 `sourceSentences` 会随内容分块一起保存到本地工作区；
- 前端直接按句子 ID 还原分块原文，不再使用段落 ID 查找句子；
- 旧工作区中的 `Pxx` 引用会自动迁移为该段落下的全部句子 ID；
- 界面不再显示内部来源编号，改为展示当前分块包含的完整原文；
- 增加原文覆盖、遗漏和重复检查，并为 ID 迁移补充自动化测试。

### 验证结果

- 任意分块均能显示对应的完整原文；
- 分块拆分和合并后仍保持原文完整覆盖；
- 页面不再暴露 `Sxx-xx` 或 `Pxx` 等内部编码；
- 示例内容覆盖结果为 `13 / 13 句`，遗漏和重复均为 `0`。

## 2. 输入输出模式与编辑器模式不同步（已修复）

**状态：已于 2026-07-28 修复。**

### 原问题

输入页使用 `project.outputMode` 保存用户选择的“精华版”或“完整卡片版”，编辑器则使用独立的 `editorMode` 状态。

用户在输入页选择“精华版”后，进入编辑器时仍可能停留在“卡片版”。

### 修复方式

- 保留“精华版”和“完整卡片版”两种输出模式；
- 使用 `project.outputMode` 作为输入页、项目列表、编辑器和导出页的唯一模式状态；
- 旧工作区中的 `editorMode` 会在加载时迁移到 `project.outputMode`，之后不再单独保存；
- 编辑器切换标签时会直接更新项目输出模式；
- 每次结构化解析都会同时生成精华发布文案和完整卡片，不再根据当前展示模式省略其中一种结果；
- 生成结果记录 `generatedModes`、`generatedSourceKey` 和发起解析时的模式；原文或项目语境变化后，旧结果自动失效；
- 缺少当前原文对应的精华文案时展示“生成精华版”空状态，不再使用完整原文填充精华正文；
- 精华版导出纯文本发布包，完整卡片版导出 PNG 图片包。

### 精华版生成与发布约束

- 标题、正文、标签及它们之间的换行合计不得超过 `1000` 字；
- 原文超过 `950` 字时，模型必须对内容重新组织和压缩，生成目标控制在约 `850–930` 字；
- 模型根据原文场景自行选择口吻，例如欢快活泼、严谨深刻、轻松活跃或更贴近日常叙事的表达；
- 提示词禁止编造原文没有的人物、地点、对话和事实细节，并抑制模板词、空洞金句及过度使用 emoji；
- 模型不可用时会生成不超过 `1000` 字的原文保真兜底稿，并明确提示这不是个性化改写；
- 编辑页实时显示总字数和剩余字数，超限时禁止复制及进入导出；
- 导出函数仍会再次校验，避免通过其他入口绕过限制。

### 验证结果

- 两种输出模式在输入、编辑、项目列表及导出环节保持一致；
- 从任一模式发起解析，返回结果均同时包含精华文案和完整卡片；
- 旧项目或已过期结果不会在切换模式后被当作当前内容使用；
- 长原文的模型兜底结果始终不超过 `1000` 字；
- 精华版发布文案按实际复制文本计算总字数。

## 3. 部分按钮只有视觉状态，没有实际行为

当前界面中存在尚未接入功能的按钮，包括：

- 添加观点；
- 添加 Block；
- 删除本页；
- 搜索；
- 使用指南；
- 偏好设置；
- 查看隐私原文；
- 刷新导出记录。

### 影响

- 用户点击后没有反馈，容易被理解为程序故障；
- 界面展示的能力超过实际实现范围；
- 无法完成观点补充、卡片结构调整等关键编辑动作。

### 建议

按业务优先级逐项接入真实行为。暂不准备实现的入口应移除、禁用，或明确标记为“开发中”，避免制造错误预期。

## 4. 校验结果主要是静态文案

### 现象

界面显示以下校验结果：

- 正文字符合规；
- 无文字溢出；
- 标题未孤立；
- 末页平衡良好；
- 页码连续；
- 字体与资源已就绪。

这些结果目前没有全部连接到真实检测逻辑。精华版的 `1000` 字发布限制已经接入真实检测和导出阻断，但卡片溢出、孤立标题、末页平衡等检查仍有部分是静态文案。

### 影响

- 用户可能在错误的安全提示下导出不合规内容；
- 卡片文字可能在最终 PNG 中被裁切；
- 导出检查无法承担发布前质量门禁的作用。

### 建议

为每项校验建立可计算结果，并把结果用于按钮阻断：

- 根据真实字符数判断精华正文是否超限；（已完成）
- 使用 DOM 实际尺寸检测卡片溢出；
- 检测空白页、孤立标题和连续页码；
- 在图片生成前等待字体与资源加载完成；
- 对失败项提供定位和修复入口。

## 5. 版本和导出记录没有持久化

### 现象

内容版本和导出记录没有存入 `LocalWorkspace`，也没有写入 `localStorage` 或后端数据库。

页面刷新后，真实历史会消失，并重新显示代码中的演示记录。版本和导出记录也没有按项目隔离。

### 影响

- 用户无法可靠恢复历史内容；
- 不同项目可能共享同一组版本和导出状态；
- 页面所展示的历史记录不一定来自用户的实际操作。

### 建议

短期可以把版本和导出记录纳入 `LocalWorkspace` 并持久化到浏览器；生产版本应接入已经创建的 `content_versions` 和 `export_packages` 数据表。

## 6. “再次下载”不是历史文件下载

### 现象

导出记录只保存文件名、主题、字符数和卡片数等元数据，没有保存当次导出的内容快照或 ZIP 文件。

点击“再次下载”时，系统会使用当前项目内容重新生成发布包，仅复用历史记录中的主题。如果项目内容已经修改，新文件不会与原始导出一致。

### 影响

- “再次下载”的名称与实际行为不一致；
- 无法复现历史发布资产；
- 用户难以确认之前真正发布过的内容。

### 建议

选择以下一种产品语义：

1. 保存每次导出的完整内容快照，并基于快照重新生成；
2. 把 ZIP 上传到私有存储，提供历史文件下载；
3. 将按钮改名为“按当前内容重新生成”，明确它不是历史文件。

## 7. AI 接入方式与项目约定不一致

### 现象

项目约定要求通过 `nubase.ai` 网关使用 AI 能力，实际语义解析代码直接调用 OpenAI Chat Completions 兼容接口，并读取模型地址、模型名称和密钥。

### 影响

- AI 调用绕过统一的平台鉴权和审计链路；
- 模型配置、错误处理和用量统计分散；
- 与项目已安装的 Nubase 运行时能力形成两套实现；
- 后续更换模型或增加权限控制时维护成本较高。

### 建议

统一 AI 接入策略。如果继续使用 Nubase，应把语义解析迁移到 `nubase.ai`；如果保留 OpenAI 兼容接口，则需要同步修改项目约定，并补充统一的鉴权、限流、日志脱敏和用量统计。

## 8. 缺少自动化测试

### 现象

当前没有发现核心业务测试文件，同时 Vitest 配置允许在没有测试时成功退出。

以下关键逻辑缺少回归保护：

- 原文句子是否完整保留；
- 句子 ID 是否遗漏或重复；
- 模型异常后的本地回退；
- 语义分页与卡片归一化；
- 项目创建、切换和自动保存；
- 版本恢复；
- 字符与溢出校验；
- ZIP 文件结构和 PNG 数量。

### 影响

- 修改拆卡算法后难以及时发现原文丢失；
- UI 状态重构容易破坏项目隔离和自动保存；
- 导出包可能缺少文件或包含错误内容。

### 建议

优先为纯函数和服务端语义解析增加单元测试，再为项目工作流和导出流程增加集成测试。测试配置不应把“零测试”作为长期可接受状态。

## 9. 主页面职责过于集中

### 现象

项目的大部分业务类型、示例数据、状态管理、项目操作、AI 调用、版本管理、导出逻辑和页面组件集中在 `src/routes/index.tsx`。

该文件规模接近 600 行，与项目约定中“复杂界面拆分为特性组件”的方向不一致。

### 影响

- 多个功能共享大量松散状态，容易出现同步问题；
- 单个功能改动可能影响整个工作流；
- 测试、复用和代码审查成本较高；
- 后续接入后端持久化时重构难度增大。

### 建议

按功能边界拆分为：

```text
src/features/projects/
src/features/input/
src/features/analysis/
src/features/editor/
src/features/export/
src/features/versioning/
```

同时把以下逻辑提取为独立模块：

- 工作区状态与持久化；
- 语义结果适配；
- 卡片分页与校验；
- PNG 和 ZIP 导出；
- 版本快照；
- 导出记录。

## 后续建议处理顺序

1. 实现其余真实导出校验；
2. 持久化版本和导出记录；
3. 明确“再次下载”的产品语义；
4. 补充核心编译和导出测试；
5. 拆分主页面职责；
6. 统一 AI 接入方式；
7. 清理或实现无行为按钮。
