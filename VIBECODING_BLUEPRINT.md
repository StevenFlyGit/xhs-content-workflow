# 小红书内容编译器 - VibeCoding 蓝图

## 项目概述
面向知识型创作者，将活动长文经过结构化解析，编译为小红书精华正文或 3:4 知识卡片，并完成校验、预览与发布包导出。首版严格控制在文档定义的内容编译器 MVP 范围内。

## 三种设计方向
### 方向 1: 结构化笔记工作台
**简介:** 以固定侧栏、流程步骤条、模块化编辑面板和状态栏组织完整编译流程。
**概率:** 0.70

### 方向 2: 双栏专业编辑器
**简介:** 以内容编辑与卡片预览为核心，弱化项目管理，强调沉浸式创作。
**概率:** 0.20

### 方向 3: 紧凑运营控制台
**简介:** 强化项目列表、任务状态和内容导出，适合高频批量内容生产。
**概率:** 0.10

## 选定方向: 结构化笔记工作台
最符合用户选择，也最能承载输入、解析、编辑、预览、校验与导出的多阶段工作流。

## 设计原则
- 首屏直接呈现可操作的创作工作区
- 高信息密度但保持清晰层级与稳定对齐
- 内容、来源证据与视觉模板相互解耦
- 每个关键动作都有可见状态、校验和反馈
- 优先保护原意、隐私约束与可追溯性

## 色彩哲学
- 浅灰 #F4F6F8 作为应用底色，白色作为主要工作面
- 深灰文字确保长文阅读舒适，蓝色作为主操作与选中状态
- 绿色表示合规或已保存，琥珀色表示提醒，红色仅用于超限和隐私风险

## 布局范式
- 左侧固定项目与流程导航
- 顶部显示项目名称、自动保存与任务状态
- 主区按阶段切换输入、解析、编辑与导出视图
- 内容编辑阶段使用左侧 Block 编辑、右侧 3:4 卡片预览
- 关键统计与校验固定在侧栏或底部操作条

## 交互哲学
- 所有标签页、模式切换、模板选择和编辑控件必须有可见反馈
- 长文输入实时计数并显示接近或超过 10000 字符的状态
- 模拟异步解析时显示进度、成功和失败可重试状态
- 结构化观点可编辑、排序、标记必留或不可公开，并可查看原文锚点
- 精华版与卡片版切换时保留各自编辑状态

## 排版系统
- 采用紧凑、清晰的中文产品界面字阶
- 页面标题克制，面板标题与正文依靠字重和间距区分
- 字符数、页码、任务状态等使用等宽数字或稳定宽度展示

## 页面结构
### 1. 项目工作台
展示最近项目、状态、输出模式、更新时间，并支持创建、复制和删除项目。

### 2. 内容输入
填写活动信息、目标读者、语气、内容约束和不超过 10000 字符的原始长文，同时显示输入状态。

### 3. 结构化解析
编辑核心观点、案例、感悟、行动建议和敏感内容，并通过段落锚点追溯原文。

### 4. 内容编辑器
在精华版正文编辑和完整卡片 Block 编辑之间切换，提供 4 套模板、字符统计与实时预览。

### 5. 导出检查
汇总正文、标签、卡片与校验结果，展示发布包文件清单并触发导出。

## 首版实现范围
- 实现完整的五阶段前端工作流与稳定导航
- 实现项目创建、长文输入、活动信息和内容约束表单
- 实现字符计数、预计卡片数、自动保存和风险提示
- 实现可编辑的结构化解析结果及原文锚点查看
- 实现精华版标题、正文、标签编辑与字符预算校验
- 实现完整卡片版 Block 编辑、语义分页演示和 4 套模板实时切换
- 实现 3:4 卡片预览、连续页码和溢出/空白页等校验状态
- 实现导出检查与发布包生成反馈

## 验收标准
- 用户能创建项目并输入最多 10000 字符的活动长文，字符数、自动保存和风险状态实时可见
- 用户能完成结构化解析结果的编辑、必留/私密标记与原文锚点追溯
- 用户能在精华版和完整卡片版之间切换，编辑内容并看到 4 套模板的实时 3:4 预览
- 导出前能看到字符、分页、隐私和资源完整性检查，并能生成包含正文、标签、卡片和结构化数据的发布包
- 关键按钮、筛选、标签、模板、表单和任务状态均具有真实可见的交互反馈

## 增强建议
### 原文证据对照
- **分类:** 内容可信度
- **说明:** 在编辑观点时并排高亮原文段落，显示生成内容与来源之间的对应关系。
- **推荐:** 是
- **需要后端:** 否
- **实现提示:** 在结构化解析视图增加锚点抽屉与文本高亮，先基于解析结果中的段落 ID 实现。

### 内容版本恢复
- **分类:** 工作流
- **说明:** 保存结构化分析、精华正文和卡片稿的历史版本，支持恢复上一版。
- **推荐:** 是
- **需要后端:** 是
- **后端模块:** database.crud
- **实现提示:** 为内容实体记录版本快照，在编辑器顶部提供版本时间线和恢复确认。

### 卡片密度调节
- **分类:** 排版体验
- **说明:** 提供舒展、标准、紧凑三档密度，并实时重新计算分页和末页平衡。
- **推荐:** 是
- **需要后端:** 否
- **实现提示:** 通过主题间距与字号 token 改变测量结果，保留语义 Block 边界。

### 发布前隐私复核
- **分类:** 内容安全
- **说明:** 在导出前集中列出命中的不可公开内容和可能敏感的段落，要求逐项确认。
- **推荐:** 是
- **需要后端:** 否
- **实现提示:** 复用用户输入的隐私约束和结构化分析结果，生成阻断式检查清单。

## 后端建议
预置真实后端模块：auth.email_password, database.crud, storage.private_bucket, server_functions.secure_action, ai.nubase_gateway。

<!-- ottermind:vibecoding-blueprint-decision:start -->
## 生成确认记录

状态: 等待用户确认
- 后端模式: 待确认
- 登录/认证: 待确认
- 数据库持久化: 待确认
- 文件上传/存储: 待确认
- 已选增强项: 待确认
- 选择: 待确认
- 来源: 待确认
- 更新时间: 待确认
<!-- ottermind:vibecoding-blueprint-decision:end -->
